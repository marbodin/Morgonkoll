Optional: place a square JPEG cover at assets/cover.jpg.

The pipeline embeds it as an ID3 attached picture. Without this file, episode
generation still succeeds and all required text metadata remains embedded.

